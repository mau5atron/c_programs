#include <stdio.h>

/*
	The following program has a problem. Enter it in your editor and 
	compile it. Which lines generate an error message? 
*/ 
int main(void)
{
	printf( "Keep looking!" ); // maybe there is a \n missing?
	printf( "You\'ll find it!\n" );
	return 0;
}