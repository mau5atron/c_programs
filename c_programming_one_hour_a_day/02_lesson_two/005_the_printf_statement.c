#include <stdio.h>

int main(void)
{
	/*
		The printf statement is a library function that displays information
		onscreen. The printf() statement can display a simple text message
		or a message mixed with the value of one or more program variables 
	*/ 
}