#include <stdio.h>

int main(void)
{
	/*
		A function prototype provides the C compiler with the name and 
		arguments of the function contained in the program. It appears before 
		the function is used. A function prototype is distinct from a 
		function definition, which contains the actual statements that make
		up the function (function definitions are discussed in more detail
		in the "Function definition" section).

	*/ 
}