#include <stdio.h>

// the ANSI C11 Standard says that you need a void if the main function
// wont return anything 
int main(void)
{
	/*
		A variable is a name assigned to a location in memory used to store
		information. Your program uses variables to store various kinds of 
		information during program execution.

		In C, a variable must be defined before it can be used. A 
		variable definition informs the compiler of the variables name and
		the type of information the variable is to hold.
	*/ 
}